@model Lanche

<div class="lanchesDesc col-sm-4 col-md-4 col-lg-4">
    <p><img src="@Model.ImagemUrl" class="img-fluid" /></p>
    <h3>Preço: @Model.Preco.ToString("c")</h3> @*formata em dinheiro, moeda local.*@
    <h3>@Model.Nome</h3>
    <p>@Model.DescricaoCurta</p>
</div>
